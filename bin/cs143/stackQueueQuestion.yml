---

###################DISPLAY IN SHOWDIALOGUE WINDOW#####################

    Type: 'UserInput'
    Question: |
        What is the output of the following lines of code?
            Stack<String> s = new Stack<String>(); 
            Queue<String> q = new LinkedList<String>(); 
            s.push("how"); 
            s.push("are"); 
            s.push("you"); 
            while(!s.isEmpty()){
                q.add(s.pop()):}
            System.out.println(q);
        Recall that stacks and queues print in an [a,b,c] format.
    CorrectAnswer: '[you, are, how]'

######## GET INPUT FROM USER#############
...